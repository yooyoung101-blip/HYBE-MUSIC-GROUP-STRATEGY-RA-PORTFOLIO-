const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];let p=0,pi=setInterval(()=>{p=Math.min(100,p+Math.ceil(Math.random()*12));$('#pct').textContent=p;if(p===100){clearInterval(pi);setTimeout(()=>$('#loader').classList.add('hide'),180)}},70);const progress=$('.progress span'),topbar=$('.topbar'),glow=$('.glow');addEventListener('scroll',()=>{let h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h?scrollY/h*100:0)+'%';topbar.classList.toggle('scrolled',scrollY>24)},{passive:true});addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'},{passive:true});const ro=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');ro.unobserve(e.target)}}),{threshold:.12});$$('.reveal').forEach(x=>ro.observe(x));const co=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;let el=e.target,n=+el.dataset.n,d=+el.dataset.d||0,start=performance.now();function tick(now){let t=Math.min(1,(now-start)/1100),v=n*(1-Math.pow(1-t,3));el.textContent=v.toFixed(d);if(t<1)requestAnimationFrame(tick)}requestAnimationFrame(tick);co.unobserve(el)}),{threshold:.55});$$('.count').forEach(x=>co.observe(x));const bo=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){$$('.bar-track span',e.target).forEach((b,i)=>setTimeout(()=>b.classList.add('go'),i*140));bo.unobserve(e.target)}}),{threshold:.3});if($('.fan-main'))bo.observe($('.fan-main'));const copy={global:['MY READ / 01','스트리밍은 계속 성장하고 있지만 지역별 성장률 차이가 큽니다. 글로벌 전략에서는 한 시장의 절대 규모와 함께 <b>증가 속도, 포맷 비중, 팬 행동</b>을 같이 봐야 합니다.'],stream:['MY READ / 02','유료 스트리밍 계정은 8억 명을 넘었습니다. DSP 지표는 단순 도달보다 <b>발견 → 저장 → 반복 청취 → 팬 전환</b> 단계로 나눠 볼수록 다음 액션이 선명해집니다.'],asia:['MY READ / 03','2025년 아시아 음반산업 매출은 10.9% 성장했습니다. 글로벌 성과는 <b>지역별 성장 속도와 현지 소비 포맷</b>을 따로 비교해야 합니다.']};$$('.metric').forEach(m=>m.onclick=()=>{$$('.metric').forEach(x=>x.classList.remove('active'));m.classList.add('active');let c=copy[m.dataset.key],i=$('#insight');$('small',i).textContent=c[0];$('p',i).innerHTML=c[1]});$$('.tilt').forEach(card=>{card.onpointermove=e=>{if(innerWidth<900)return;let r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(700px) rotateX(${y*-4}deg) rotateY(${x*5}deg) translateY(-3px)`};card.onpointerleave=()=>card.style.transform=''});const mb=$('#menuBtn'),mm=$('#mobileMenu');mb.onclick=()=>{let o=mm.classList.toggle('open');mb.textContent=o?'CLOSE':'MENU';document.body.style.overflow=o?'hidden':''};$$('a',mm).forEach(a=>a.onclick=()=>{mm.classList.remove('open');mb.textContent='MENU';document.body.style.overflow=''})